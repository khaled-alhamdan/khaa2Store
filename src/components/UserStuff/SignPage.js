import React from "react";
import { observer } from "mobx-react";

export const SignPage = () => {
  return (
    <div>
      <h1> This sign up/in page</h1>
    </div>
  );
};

export default observer(SignPage);
